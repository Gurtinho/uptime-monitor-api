{
  "name": "uptime-monitor-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/shared/infra/http/server.ts",
    "test": "vitest",
    "test:run": "vitest run",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage",
    "test:e2e": "docker compose --env-file .env.test -f docker-compose.test.yml up -d --wait && vitest run --config vitest.e2e.config.ts",
    "test:e2e:watch": "docker compose --env-file .env.test -f docker-compose.test.yml up -d --wait && vitest --config vitest.e2e.config.ts",
    "migrate:dev": "docker compose up -d --build && prisma migrate dev",
    "migrate": "docker compose up -d --build && prisma migrate deploy",
    "lint": "eslint . --ext .ts,.tsx,.js --max-warnings=0",
    "lint:fix": "eslint . --ext .ts,.tsx,.js --fix",
    "format": "prettier --write \"src/**/*.{ts,tsx,js,json,md}\"",
    "build": "yarn format && yarn lint:fix && tsup src/shared/infra/http/server.ts --format esm"
  },
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "7",
    "bullmq": "^5.76.5",
    "dotenv": "^17.4.2",
    "eslint-config-prettier": "^10.1.8",
    "express": "^5.2.1",
    "express-async-errors": "^3.1.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.6.0",
    "@types/supertest": "^7.2.0",
    "@types/swagger-jsdoc": "^6.0.4",
    "@types/swagger-ui-express": "^4.1.8",
    "@typescript-eslint/eslint-plugin": "^8.59.1",
    "@typescript-eslint/parser": "^8.59.1",
    "@vitest/coverage-v8": "4.1.5",
    "eslint": "^10.3.0",
    "eslint-import-resolver-typescript": "^4.4.4",
    "eslint-plugin-import": "^2.32.0",
    "eslint-plugin-prettier": "^5.5.5",
    "prettier": "^3.8.3",
    "prisma": "7",
    "supertest": "^7.2.2",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  }
}
